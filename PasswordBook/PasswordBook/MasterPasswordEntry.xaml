<UserControl x:Class="PasswordBook.MasterPasswordEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Background="White">
    <Grid Margin="11">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <PasswordBox Grid.Row="0" x:Name="password"/>
        <Button Content="Submit" Command="{Binding SubmitCommand}" Grid.Row="1" Margin="0,5,0,0"/>
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Grid.Row="3"/>
    </Grid>
</UserControl>
